/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type TransferFromPoolInstructionAccounts = {
  vaultType?: PublicKey | Pda;
  authority?: Signer;
  mint: PublicKey | Pda;
  pool?: PublicKey | Pda;
  destination: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type TransferFromPoolInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
};

export type TransferFromPoolInstructionDataArgs = { amount: number | bigint };

export function getTransferFromPoolInstructionDataSerializer(): Serializer<
  TransferFromPoolInstructionDataArgs,
  TransferFromPoolInstructionData
> {
  return mapSerializer<
    TransferFromPoolInstructionDataArgs,
    any,
    TransferFromPoolInstructionData
  >(
    struct<TransferFromPoolInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
      ],
      { description: 'TransferFromPoolInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([136, 167, 45, 66, 74, 252, 0, 16]),
    })
  ) as Serializer<
    TransferFromPoolInstructionDataArgs,
    TransferFromPoolInstructionData
  >;
}

// Args.
export type TransferFromPoolInstructionArgs =
  TransferFromPoolInstructionDataArgs;

// Instruction.
export function transferFromPool(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: TransferFromPoolInstructionAccounts & TransferFromPoolInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'vault',
    'Ds1WLBK4R44S3bk1DeDiUrwxQ9ydrdjn7hkrY61EDVXF'
  );

  // Accounts.
  const resolvedAccounts = {
    vaultType: {
      index: 0,
      isWritable: false as boolean,
      value: input.vaultType ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    mint: { index: 2, isWritable: false as boolean, value: input.mint ?? null },
    pool: { index: 3, isWritable: true as boolean, value: input.pool ?? null },
    destination: {
      index: 4,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: TransferFromPoolInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.vaultType.value) {
    resolvedAccounts.vaultType.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([118, 97, 117, 108, 116, 95, 116, 121, 112, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.mint.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.pool.value) {
    resolvedAccounts.pool.value = context.eddsa.findPda(programId, [
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.vaultType.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.tokenProgram.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.mint.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTransferFromPoolInstructionDataSerializer().serialize(
    resolvedArgs as TransferFromPoolInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
